# Sparse Frontier runtime stack for Leonardo. Download these wheels on the
# internet-connected login node, then install them offline into the target
# conda env on the GPU node. The default vLLM/FlashInfer path does not need
# a separate flash-attn install; the optional minference CUDA extension is
# built locally by the offline installer.
accelerate>=1.0
datasets>=2.16.0,<5
flashinfer-python==0.5.2
flashinfer-cubin==0.5.2
hydra-core>=1.3,<2
numpy>=1.23
omegaconf>=2.3,<3
PyYAML>=6
sentencepiece==0.2.0
transformers==4.57.1
vllm==0.11.0
wonderwords

# Local lm-evaluation-harness editable install dependencies.
dill
evaluate>=0.4.0
jinja2
jsonlines
more_itertools
pytablewriter
rouge-score>=0.0.4
sacrebleu>=1.5.0
scikit-learn>=0.24.1
sqlitedict
typing_extensions
word2number
zstandard
